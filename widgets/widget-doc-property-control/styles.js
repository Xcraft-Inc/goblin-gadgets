export default function styles() {
  const control = {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'flex-start',
  };

  return {
    control,
  };
}

/******************************************************************************/
