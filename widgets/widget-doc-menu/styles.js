export default function styles() {
  const container = {
    display: 'flex',
    flexDirection: 'column',
  };

  const title = {
    marginTop: '18px',
    marginBottom: '18px',
  };

  return {
    container,
    title,
  };
}

/******************************************************************************/
