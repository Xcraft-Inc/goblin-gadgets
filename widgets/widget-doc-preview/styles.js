export default function styles() {
  const container = {
    margin: '10px 0px',
    display: 'flex',
    flexDirection: 'column',
  };

  const code = {
    margin: '5px 0px',
    whiteSpace: 'normal',
    userSelect: 'text',
  };

  const settings = {
    margin: '10px 0px',
  };

  return {
    container,
    code,
    settings,
  };
}

/******************************************************************************/
