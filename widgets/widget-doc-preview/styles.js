export default function styles() {
  const container = {
    margin: '5px 0px',
    padding: '10px',
    display: 'flex',
    flexDirection: 'column',
    backgroundColor: '#fff',
  };

  const code = {
    userSelect: 'text',
    overflow: 'auto',
  };

  const settings = {
    margin: '10px 0px',
  };

  return {
    container,
    code,
    settings,
  };
}

/******************************************************************************/
